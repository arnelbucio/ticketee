<span class="tag">
  <%= tag.name %>
  <% if can?(:tag, @ticket.project) || current_user.admin? %>
    <%= link_to "x",
        ticket_tag_path(@ticket, tag),
        method: :delete,
        remote: true,
        id: "delete-#{tag.name.parameterize}" %>
  <% end %>
</span>
